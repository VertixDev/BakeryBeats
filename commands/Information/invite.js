const Discord = require("discord.js")

module.exports = {
    name: "invite",
    description: "Provides an invite link to invite Bakery Beats to your own server!",
    timeout: 1000,
    run: async(client, message, args) => {
        
    }
}